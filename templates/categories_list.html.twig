{% extends 'base.html.twig' %}



{% block main %}
    {% for category in categories %}
    <article>
        <h2 style="color: {{ category.color }};">{{ category.title }}</h2>
        <a href="{{ path('show_category', {'id': category.id}) }}">Voir plus</a>
    </article>
    {% endfor %}
{% endblock %}


{% block footer %}
    <footer>Coucou, je suis le footer</footer>
{% endblock %}

